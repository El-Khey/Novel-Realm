# URL de base de l'API backend (utilisée en développement).
# IMPORTANT : avec Vite, seules les variables préfixées par VITE_ sont
# exposées au code frontend (import.meta.env.VITE_API_URL).
VITE_API_URL=http://localhost:8080
