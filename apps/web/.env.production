# Variables utilisées lors du build de PRODUCTION (`npm run build`).
# Le navigateur (sur la machine hôte) joint l'API via le port 8080 exposé
# par Docker → on garde la même URL qu'en dev.
VITE_API_URL=http://localhost:8080
